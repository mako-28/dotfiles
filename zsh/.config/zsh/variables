export EDITOR=nvim
export SUDO_EDITOR=nvim
export JDTLS_JVM_ARGS="-javaagent:$HOME/lima/src/lib/lombok.jar"
if [[ -f ${HOME}/.github_api_token ]]; then
    export HOMEBREW_GITHUB_API_TOKEN=$(cat ${HOME}/.github_api_token)
fi
